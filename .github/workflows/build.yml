on:
  push:
    branches:
      - custom  # Specify only the 'dev' branch
  pull_request:
    branches:
      - custom  # Specify only the 'dev' branch

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
